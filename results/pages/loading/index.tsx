import classNames from "classnames";

const Loading = () => (
  <section className="mt-4 flex flex-wrap justify-start gap-x-8 gap-y-4"></section>
);

Loading.pageTitle = "";
Loading.pageDescription = "";

export default Loading;
